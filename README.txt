✅ Whisper GPU Meeting Transcriber

📦 시스템 요구:
- NVIDIA GPU
- GPU 드라이버 설치됨
- CUDA 설치 불필요 (내장 DLL 자동 로딩됨)

📁 사용법:
1. WhisperGUI.exe 실행
2. 모델 선택 후 오디오 파일 선택
3. 출력 폴더 지정 → 전사 시작

❗ 문제가 생긴다면:
- cuda/bin 폴더가 꼭 exe와 함께 있어야 함
- GPU 드라이버가 없는 경우 실행되지 않음